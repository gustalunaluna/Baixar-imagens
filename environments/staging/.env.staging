# ──────────────────────────────────────────────────────────────────
# CMS — AMBIENTE DE STAGING (TESTES)
# ──────────────────────────────────────────────────────────────────
# ATENÇÃO: Este arquivo NÃO deve ser commitado com valores reais.
# Preencha com as credenciais do projeto Supabase de STAGING
# (crie um projeto separado em supabase.com para testes).
# ──────────────────────────────────────────────────────────────────

# Supabase — projeto de STAGING (diferente do de produção)
NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO-STAGING.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9_STAGING_ANON
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9_STAGING_SERVICE_ROLE

# URL base do ambiente de staging
NEXT_PUBLIC_SITE_URL=https://cms-staging.lsconfex.com.br

# Identifica o ambiente para lógica condicional no código
NEXT_PUBLIC_ENV=staging

# Admin inicial para setup (use variáveis, nunca hardcode)
ADMIN_EMAIL=admin-staging@lsconfex.com.br
ADMIN_PASSWORD=TROQUE_ESTA_SENHA_ANTES_DE_USAR
